import React from 'react'
import ReactDOM from 'react-dom'
import App from './App'

const el = document.getElementById('root')
const root = ReactDOM.render(el!)

root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
)
